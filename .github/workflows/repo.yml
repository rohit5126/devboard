name: repo-dispatch
on: 
  repository_dispatch:
    types: [deploy-event]

jobs:
 execute-dispatch:
   runs-on: ubuntu-latest
   steps:
     - name: print
       run: echo "${{ github.event.client_payload.environment }}"
     
