name: devboard
on:
  push:
    branches: master
    paths: .github/workflows/devboard.yml
jobs:
  CI:
    runs-on: [self-hosted, linux]
    steps:
      - name: code-checkout
        run: actions/checkout@v7
