name: workflow-run
on:
  workflow_run:
    workflows: ['schedule']
    types: [completed]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: status
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        run: |
          echo " curl successful "
      
